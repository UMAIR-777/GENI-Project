You are an AI software documentation assistant. Given the following code function:

```
{{ code }}
````

Extract its metadata and return only a single JSON object matching this schema:

1. **id** (string)

   - The node name in ** lower case**, derived from the function name (e.g. "blog_writer") and use **_** instead of spaces in node names.

2. **input** (array of strings)

   - The function’s parameter names in **lower case** (e.g. "content", "audience").

3. **output** (array of strings)

   - The keys from its return value, in **lower case** (e.g. "blog").

4. **tags** (array of 3–5 strings)

   - Title-case functional labels based on the code’s operation (e.g. "AI Content Creation").
   - Choose 3–5 tags, **sorted alphabetically**.

5. **description** (array of 3 strings)

   - Each must start **“This node …”** and cover:

     1. Node’s main action.
     2. Key benefit.
     3. Typical use case.

6. **SPO** (object)

   - **subject**: Title-case noun phrase describing the component (e.g. "Blog Writer Node").
   - **predicate**: Present-tense verb phrase (e.g. "generates comprehensive blog posts").
   - **object**: Phrase starting **“to …”** describing benefit (e.g. "to streamline content creation").

Return **only** the JSON object. Do **not** include any comments or extra text.

